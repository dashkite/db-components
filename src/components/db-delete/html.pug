main
  .block(class = "section")
    section 
      header
        h2 Delete Database?
      .main This action can't be undone.
      footer
        if loading
          .loading 
        else 
          nav 
            a(name="cancel" href="#") Cancel
            button.highlight Delete Database